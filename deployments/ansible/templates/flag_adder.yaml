http_host: "{{ flag_adder_host }}" #default -- 0.0.0.0
http_port: "{{ flag_adder_port }}" #default - 5000
redis_host: "{{ redis_host }}" #default -- 127.0.0.1
redis_port: "{{ redis_port }}" #default -- 6379
redis_pool_size: 5 #default -- 5
flag_prefix: "{{ flag_prefix }}"
